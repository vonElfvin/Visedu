import { ProblemFilterPipe } from './problem-filter.pipe';

describe('ProblemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProblemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
