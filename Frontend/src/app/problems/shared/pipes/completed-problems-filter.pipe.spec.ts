import { CompletedProblemsFilterPipe } from './completed-problems-filter.pipe';

describe('CompletedProblemsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedProblemsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
